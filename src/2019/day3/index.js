const read = require('../../utils/read');
const data = read(__dirname).split('\n');

const path1 = data[0].split(',');
const path2 = data[1].split(',');

for (let instr of path1) {
	let dir = inst.substr(0, 1);
	let len = inst.substr(2);
	for (let i = 0; i < len)

}